{{#camelize}}{{name}}{{/camelize}}Controller = require 'controllers/{{name}}_controller'

describe '{{#camelize}}{{name}}{{/camelize}}Controller', ->
    beforeEach ->
        @controller = new {{#camelize}}{{name}}{{/camelize}}Controller
    afterEach ->
        @controller.dispose()
